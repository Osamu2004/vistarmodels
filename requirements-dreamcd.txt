# DreamCD official requirement.txt pins torch==1.8.1+cu111. Install torch and
# torchvision separately for your CUDA driver, preferably in a dedicated env.
albumentations
einops
imageio
huggingface-hub
kornia
numpy
omegaconf
opencv-python
pillow
pytorch-lightning
termcolor
test-tube
torch-fidelity
torchmetrics
tqdm
# The upstream package uses namespace-style source directories without root
# __init__.py files. A normal wheel built by modern pip is effectively empty,
# so keep the official source checkout editable and pin it for reproducibility.
-e git+https://github.com/CompVis/taming-transformers.git@3ba01b241669f5ade541ce990f7650a3b8f65318#egg=taming-transformers
transformers
# Use OpenAI's CLIP implementation; the unrelated PyPI package named `clip`
# is not compatible with latent-diffusion.
clip @ git+https://github.com/openai/CLIP.git@d05afc436d78f1c48dc0dbf8e5980a9d471f35f6
